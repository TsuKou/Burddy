<div class="container">
  <div class="row justify-content-center">
    <h2 class="title-background my-5">レビュー一覧</h2>
  </div>

  <div class="row justify-content-center">
    <div class="col">
      <table class="table">
        <tr>
          <th>レビューID</th>
          <th>投稿日時</th>
          <th>店舗名</th>
          <th>ユーザー名</th>
          <th>会員氏名</th>
          <th>レビュータイトル</th>
        </tr>
        <% @reviews.each do |review| %>
        <tr>
          <td><%= review.id %></td>
          <td><%= review.created_at.strftime('%Y/%m/%d') %></td>
          <td><%= link_to review.shop.shop_name.truncate(5), admin_shop_path(review.shop_id) %></td>
          <td><%= link_to review.user.user_name, admin_user_path(review.user_id) %></td>
          <td><%= review.user.full_name %></td>
          <td><%= link_to review.review_title.truncate(10), admin_review_path(review.id) %></td>
          <td><%= link_to "削除", admin_review_path(review), method: :delete, data: {confirm: "投稿を削除します。よろしいですか？"}, class: 'btn btn-danger' %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>

<div><%#= link_to "削除", admin_review_path(review), method: :delete, data: { confirm: '本当に削除しますか?' } %></div>
